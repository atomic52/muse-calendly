{
  "name": "muse-calendly",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Calendly -> Meta Muse custom connector: zero-code experiment + tooling",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "smoke": "tsx src/smoke.ts",
    "smoke:book": "tsx src/smoke.ts --book --cancel-after",
    "build:spec": "tsx src/build-spec.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
